apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-mpioutput-16
  labels:
    type: local
spec:
  #storageClassName: local-path
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/gpfs/bsc_home/xpliu/k8shpcresults"
  
---

apiVersion: batch.volcano.sh/v1alpha1
kind: Job
metadata:
  name: mpi-benchmark-16
spec:
  schedulerName: volcano
  plugins:
    ssh: []
    svc: []
# Comment out the following section to enable volumes for job input/output.
  volumes:
    - mountPath: "/home"
      #volumeClaimName: "mpi-pvc"
      volumeClaim:
        accessModes: [ "ReadWriteMany" ]
        resources:
          requests:
            storage: 1Gi
  tasks:
    - replicas: 1
      name: mpimaster
      policies:
        - event: TaskCompleted
          action: CompleteJob
      template:
        metadata:
          annotations:
            volcano.sh/task-priority: "1"
        spec:
          containers:
            - command:
                - /bin/sh
                - -c
                - |
                  MPI_HOST=`cat /etc/volcano/mpiworker.host | tr "\n" ","`;
                  mkdir -p /var/run/sshd; /usr/sbin/sshd;
                  cp /opt/results/hpccinf.txt /home;
                  sleep 60;
                  mpirun --allow-run-as-root --host ${MPI_HOST} --nooversubscribe --mca mpi_yield_when_idle 0 --mca btl ^openib,uct --mca pml ucx --display-devel-map --display-allocation --report-bindings --bind-to none -np 16 /opt/HPCC1.5.0-MPI/bin/hpccfft > hpccfft-16.log 2>&1;
                  mv hpccoutf.txt hpccfft-16.out;
                  sleep 5;
                  mpirun --allow-run-as-root --host ${MPI_HOST} --nooversubscribe --mca mpi_yield_when_idle 0 --mca btl ^openib,uct --mca pml ucx --display-devel-map --display-allocation --report-bindings --bind-to none -np 16 /opt/HPCC1.5.0-MPI/bin/hpccrandomaccess > hpccrandomaccess-16.log 2>&1;
                  mv hpccoutf.txt hpccrandomaccess-16.out;
                  sleep 5;
                  mpirun --allow-run-as-root --host ${MPI_HOST} --nooversubscribe --mca mpi_yield_when_idle 0 --mca btl ^openib,uct --mca pml ucx --display-devel-map --display-allocation --report-bindings --bind-to none -np 16 /opt/HPCC1.5.0-MPI/bin/hpccrandomring > hpccrandomring-16.log 2>&1;
                  mv hpccoutf.txt hpccrandomring-16.out;
                  sleep 5;
                  mpirun --allow-run-as-root --host ${MPI_HOST} --nooversubscribe --mca mpi_yield_when_idle 0 --mca btl ^openib,uct --mca pml ucx --display-devel-map --display-allocation --report-bindings --bind-to none -np 16 /opt/HPCC1.5.0-MPI/bin/hpccdgemm > hpccdgemm-16.log 2>&1;
                  mv hpccoutf.txt hpccdgemm-16.out;
                  sleep 5;
                  mpirun --allow-run-as-root --host ${MPI_HOST} --nooversubscribe --mca mpi_yield_when_idle 0 --mca btl ^openib,uct --mca pml ucx --display-devel-map --display-allocation --report-bindings --bind-to none -np 16 /opt/HPCC1.5.0-MPI/bin/hpccptrans > hpccptrans-16.log 2>&1;
                  mv hpccoutf.txt hpccptrans-16.out;
                  sleep 5;
                  mpirun --allow-run-as-root --host ${MPI_HOST} --nooversubscribe --mca mpi_yield_when_idle 0 --mca btl ^openib,uct --mca pml ucx --display-devel-map --display-allocation --report-bindings --bind-to none -np 16 /opt/HPCC1.5.0-MPI/bin/hpccstream > hpccstream-16.log 2>&1;
                  mv hpccoutf.txt hpccstream-16.out;
              image: mpinetworktrace
              name: mpimaster
              ports:
                - containerPort: 22
                  name: mpijob-port
              workingDir: /home
              imagePullPolicy: IfNotPresent
              securityContext:
                capabilities:
                  add: [ "IPC_LOCK" ]
              resources:
                limits:
                  rdma/hca_shared_devices_a: 1
          restartPolicy: OnFailure
    - replicas: 16
      name: mpiworker
      template:
        metadata:
          annotations:
            volcano.sh/task-priority: "2"
        spec:
          containers:
            - command:
                - /bin/sh
                - -c
                - |
                  cp /opt/results/hpccinf.txt /home;
                  mkdir -p /var/run/sshd; /usr/sbin/sshd -D;
              image: mpinetworktrace
              name: mpiworker
              ports:
                - containerPort: 22
                  name: mpijob-port
              workingDir: /home
              imagePullPolicy: IfNotPresent
              securityContext:
                capabilities:
                  add: [ "IPC_LOCK" ]
              resources:
                limits:
                  rdma/hca_shared_devices_a: 1
          restartPolicy: OnFailure
---
